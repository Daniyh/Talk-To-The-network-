flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
gunicorn==22.0.0

# CrewAI + LiteLLM (handles Groq under the hood)
# crewai-tools is intentionally excluded — it pulls langchain/openai/embedchain
# which break Railway builds. Only crewai core is needed.
crewai==0.80.0

# Explicit pydantic pin (crewai dependency, used in csv_tool.py)
pydantic>=2.0.0,<3.0.0

# Data handling
pandas==2.2.3
numpy==1.26.4
